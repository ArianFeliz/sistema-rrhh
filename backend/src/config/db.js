const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); const conectarDB = async () => { try { const client = await pool.connect(); console.log('✅ Conectado a PostgreSQL (Neon) exitosamente'); client.release(); return pool; } catch (error) { console.error('❌ Error al conectar a PostgreSQL:', error.message); process.exit(1); } }; const getPool = () => { if (!pool) { throw new Error('La base de datos no está conectada.'); } return pool; }; module.exports = { conectarDB, getPool, pool };